<h1>Host::Listings#index</h1>
<p>Find me in app/views/host/listings/index.html.erb</p>

<%= link_to "Create Listing", new_host_listing_path %>

<br/>

<% @listings.each do |l| %>
  <%= link_to l.title, host_listing_path(l) %>
  <%= button_to "Delete", host_listing_path(l), method: :delete %>
<% end %>
